# JavaMagick
JavaMagick is a Java interface to ImageMagick. It implements the most common image manipulation methods in a user friendly manner. It uses a JNI interface between Java and the MagickWand API.
